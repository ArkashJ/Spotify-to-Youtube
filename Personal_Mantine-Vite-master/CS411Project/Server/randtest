const playlist = require("./Casual Playlist.json")
console.log(playlist.tracks[0].duration_ms)
console.log(playlist.tracks[0].name)
console.log(playlist.tracks[0].album.name)
console.log(playlist.tracks[0].album.id)
console.log(playlist.tracks[0].album.artists[0].name)

var dict = {
    "duration"  : [],
    "songName"  : [],
    "albumName" : [],
    "albumId"   : [],
    "artist"    : []
}

console.log(dict.duration.push(1))
for (let i = 0; i < playlist.tracks.length; i++) {

    dict.duration.push(playlist.tracks[i].duration_ms)
    // console.log(playlist.tracks[i].duration_ms)
    // console.log(playlist.tracks[i].name)
    // console.log(playlist.tracks[i].album.name)
    // console.log(playlist.tracks[i].album.id)
    // console.log(playlist.tracks[i].album.artists[0].name)
}


//console.log(playlist)
//console.log(playlist.tracks)

// var GenerateSchema = require('generate-schema')
// var schema = GenerateSchema.mongoose(playlist.tracks)
// console.log(schema)
// // console.log(typeof schema)
// // console.log(schema)
// var goodSchema = {};
// //goodSchema[0] = schema[0]

// goodSchema["id"] = schema[0].id
// goodSchema["name"] = schema[0].name

// console.log(goodSchema)

// console.log(schema[0].album.album_type)
// //console.log(goodSchema.)

